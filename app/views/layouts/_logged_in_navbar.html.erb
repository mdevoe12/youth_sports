<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
			</button>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
        <strong><a class="navbar-brand" href="/">RecruitMe</a></strong>
			<ul class="nav navbar-nav navbar-right">
				<li><%= link_to "Athletes", player_profiles_path %></li>
        <li><%= link_to "Recruiters", recruiter_profiles_path %></li>
        <li><%= link_to "Coaches", coach_profiles_path %></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.username %><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <% unless current_user.class == User || current_user.class == Admin || current_user.type.nil? || current_user.profile.nil? %>
              <% if current_user.type == "Player"  %>
              <li><%= link_to "Your Profile", player_profile_path(current_user.profile) %></li>
              <% elsif current_user.type == "Coach" %>
              <li><%= link_to "Your Profile", coach_profile_path(current_user.profile) %></li>
              <% elsif current_user.type == "Recruiter" %>
              <li><%= link_to "Your Profile", recruiter_profile_path(current_user.profile) %></li>
              <% end %>
            <% end %>
            <li><%= link_to "Dashboard", dashboard_index_path %></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to "Logout", logout_path %></li>
          </ul>
        </li>
			</ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>
